# Prompts 11 a 24

Este arquivo preserva a sequência futura do laboratório. Ele não autoriza execução em lote: cada prompt deve ser executado individualmente, após revisão e confirmação do aluno, respeitando `AGENTS.md`.

## Prompt 11 — Pacote de contratos em build time

```markdown
Leia `AGENTS.md` e as lições 09 e 10. Execute somente a etapa 11.

Crie `packages/contracts` como um pacote TypeScript real chamado `@mfe-lab/contracts`, construído com Rslib ou outra configuração mínima já aprovada para bibliotecas neste ecossistema. Ele será consumido inicialmente via `workspace:*`.

O pacote deve exportar apenas contratos framework-agnostic:
- tipo das opções de montagem do Account;
- tipo do retorno/unmount do Account;
- constantes de nomes de eventos do laboratório;
- payload tipado para evento de carrinho;
- payload tipado para evento de perfil;
- utilitários de type guard apenas se forem realmente usados.

Requisitos:
- sem React;
- sem Vue;
- sem acesso ao DOM no import do pacote;
- saída ESM e tipos `.d.ts`;
- `exports` explícito no `package.json`;
- package privado por enquanto, mas estruturalmente publicável;
- nenhuma dependência runtime desnecessária;
- testes unitários pequenos para qualquer type guard runtime criado.

Atualize shell, Products e Account para consumir os mesmos contratos e remova as duplicações locais. Use `workspace:*` nesta etapa.

Crie `docs/lessons/11-build-time-contract-package.md` explicando:
- por que contratos compartilhados são diferentes de compartilhar estado;
- por que esse pacote é resolvido em install/build time;
- o que acontece se um app estiver compilado contra contrato antigo;
- por que TypeScript não valida payload recebido da rede/runtime sozinho;
- diferença entre `workspace:*` e uma versão publicada.

Execute build do pacote antes dos apps, depois typecheck e build completo. Atualize `docs/progress.md`.
```

### Antes de seguir, saiba responder

- Esse pacote é baixado pelo navegador como remote?
- Alterar contracts exige rebuild dos consumidores?
- Compartilhar tipos garante compatibilidade em runtime?

## Prompt 12 — Comunicação com Custom Events

```markdown
Leia `AGENTS.md` e a lição 11. Execute somente a etapa 12.

Implemente comunicação mínima e tipada entre os micro frontends usando Custom Events do navegador e os contratos de `@mfe-lab/contracts`.

Fluxo 1 — carrinho:
- ao clicar em `Adicionar` dentro de Products, além de atualizar seu contador local, o remote deve emitir um evento `cart updated` com o total atual;
- o shell deve ouvir esse evento e mostrar `Carrinho: N` no header;
- o shell não deve importar estado interno de Products.

Fluxo 2 — perfil:
- ao alternar o papel no Account Vue, o remote deve emitir um evento `profile updated` com nome e papel atuais;
- o shell deve ouvir e mostrar `Usuário: Denis · Papel` no header;
- o shell não deve acessar refs internas do Vue.

Requisitos:
- nomes e payloads vêm de `@mfe-lab/contracts`;
- listeners do shell devem ser removidos no cleanup;
- eventos devem usar `CustomEvent` com `detail` tipado;
- não usar Zustand, Redux, Pinia, RxJS ou singleton global;
- não armazenar esses valores em localStorage;
- não criar um event bus de framework;
- manter a lógica pequena e explícita;
- documentar que Custom Events são um contrato runtime e não uma solução universal.

Crie `docs/lessons/12-cross-mfe-events.md` com um diagrama de sequência e uma seção comparando:
- props/callbacks;
- Custom Events;
- URL;
- backend;
- estado global compartilhado.

Valide navegação, cleanup e os dois eventos. Adicione testes unitários apenas onde forem realmente úteis; os testes E2E completos virão depois. Atualize `docs/progress.md`.
```

### Antes de seguir, saiba responder

- Quem é a fonte da verdade do contador nesta demonstração?
- Por que o shell escuta um evento em vez de importar o store de Products?
- Em um sistema real, quais dados deveriam estar no backend em vez do browser?

## Prompt 13 — Pacote de design tokens

```markdown
Leia `AGENTS.md` e a lição 12. Execute somente a etapa 13.

Crie `packages/design-tokens` como pacote `@mfe-lab/design-tokens`, consumido via `workspace:*` por shell, Products e Account.

O pacote deve ser framework-agnostic e pequeno.

Exporte:
- um arquivo CSS público com variáveis para:
  - cores de superfície, texto, borda e destaque;
  - espaçamentos 1, 2, 3 e 4;
  - raio pequeno e médio;
  - fonte padrão do sistema;
- um módulo TypeScript com os mesmos nomes ou metadados úteis somente se houver uso real;
- uma constante de versão didática `DESIGN_TOKENS_VERSION = '1.0.0-lab'`.

Requisitos:
- remover cores e espaçamentos duplicados dos três apps quando fizer sentido;
- manter CSS Module no React e `scoped` no Vue;
- os tokens podem ser globais, mas seletores de componentes não;
- não criar tema escuro;
- não adicionar Sass, Tailwind, styled-components ou CSS-in-JS;
- não transformar tokens em Module Federation remote;
- deixar visualmente claro que os três apps usam a mesma base de tokens.

Crie `docs/lessons/13-design-tokens-package.md` explicando:
- por que React e Vue podem consumir o mesmo pacote CSS;
- por que isso não exige Module Federation;
- build time versus runtime;
- tokens versus componentes;
- vantagens e riscos de versionar tokens.

Execute build do pacote e checks dos apps. Atualize `docs/progress.md`.
```

### Antes de seguir, saiba responder

- Por que este pacote funciona nos dois frameworks?
- Mudar um token publicado atualiza o remote automaticamente?
- Qual parte continua específica de React ou Vue?

## Prompt 14 — Biblioteca de UI React

```markdown
Leia `AGENTS.md` e a lição 13. Execute somente a etapa 14.

Crie `packages/ui-react` como pacote `@mfe-lab/ui-react`, construído como biblioteca e consumido via `workspace:*` apenas por `shell-react` e `products-react`.

Componentes permitidos, e somente estes:
1. `LabButton`;
2. `AppBoundaryLabel`.

Requisitos:
- React e ReactDOM como `peerDependencies`, não empacotados dentro da biblioteca;
- TypeScript estrito;
- exports explícitos;
- estilos pequenos baseados em `@mfe-lab/design-tokens`;
- sem dependências visuais externas;
- suporte a `className` quando fizer sentido;
- props enxutas e tipadas;
- acessibilidade básica de botão;
- exportar `UI_REACT_VERSION = '1.0.0-lab'` para o experimento de versões;
- shell e Products devem substituir somente seus botões/labels equivalentes por esses componentes;
- Account Vue não deve consumir `ui-react`.

Não exponha `ui-react` por Module Federation. Não adicione o pacote na configuração `shared` apenas para fazê-lo aparecer em runtime.

Crie `docs/lessons/14-react-ui-npm-package.md` explicando:
- `dependencies`, `devDependencies` e `peerDependencies`;
- por que React não deve ser duplicado dentro da lib;
- por que Vue não consome naturalmente um componente React;
- por que atualizar a lib exige nova instalação/build dos consumidores;
- diferença entre `ui-react` e `ProductApp` remoto.

Execute build da lib, typecheck e build dos consumidores. Atualize `docs/progress.md`.
```

### Antes de seguir, saiba responder

- Por que React está em peerDependencies?
- Por que ui-react não é um micro frontend?
- O que precisa acontecer para o shell receber uma nova versão da lib?

## Prompt 15 — Web Component compartilhado

```markdown
Leia `AGENTS.md` e a lição 14. Execute somente a etapa 15.

Crie `packages/ui-web` como pacote `@mfe-lab/ui-web` com exatamente um Web Component nativo chamado `<lab-status-chip>`.

Objetivo:
Demonstrar um componente distribuído por pacote npm que pode ser usado tanto em React quanto em Vue sem transformar um framework no outro.

Requisitos:
- implementar com Custom Elements nativos e TypeScript; não adicionar Lit nesta primeira versão;
- usar Shadow DOM para isolar o estilo interno;
- atributos/propriedades mínimos: `label` e `status` (`neutral`, `success` ou `warning`);
- estilo baseado nas variáveis de `@mfe-lab/design-tokens`, com fallbacks;
- registro explícito e idempotente do custom element;
- exportar `UI_WEB_VERSION = '1.0.0-lab'`;
- fornecer tipos necessários para uso no JSX React e para o template Vue sem recorrer a `any`;
- configurar Vue para reconhecer o custom element, se necessário;
- usar o chip no shell e no Account Vue;
- não usar Module Federation para distribuir esse componente;
- não criar um segundo componente.

Crie `docs/lessons/15-web-component-package.md` explicando:
- por que Web Components atravessam frameworks;
- Shadow DOM e CSS variables;
- limites de DX, eventos, formulários e SSR;
- quando preferir wrappers específicos por framework;
- por que ainda é um pacote build-time.

Execute todos os checks. Atualize `docs/progress.md`.
```

### Antes de seguir, saiba responder

- O Web Component é renderizado por React ou pelo navegador?
- Por que ele funciona em Vue?
- Shadow DOM impede o uso de CSS variables externas?
- Uma nova versão aparece sem rebuild do app consumidor?

## Prompt 16 — Registry npm local com Verdaccio

```markdown
Leia `AGENTS.md` e as lições 11, 13, 14 e 15. Execute somente a etapa 16.

Objetivo:
Parar de depender de links `workspace:*` nos apps e simular o fluxo real de publicação/instalação por versão, sem tocar no npm público.

Configure um registry Verdaccio local em `infra/verdaccio`, executado por Docker Compose na porta 4873.

Requisitos de segurança e escopo:
- o registry deve ser apenas local para o laboratório;
- não solicitar nem armazenar token do npm público;
- configurar `.npmrc` para enviar somente o escopo `@mfe-lab` ao registry local;
- dependências públicas como React devem continuar vindo do registry público padrão;
- permitir publish local de forma simples e documentada;
- não expor o serviço para internet;
- não inserir credenciais reais no git.

Prepare para publicação local, com versões sem sufixo didático:
- `@mfe-lab/contracts@1.0.0`;
- `@mfe-lab/design-tokens@1.0.0`;
- `@mfe-lab/ui-react@1.0.0`;
- `@mfe-lab/ui-web@1.0.0`.

Ajuste os pacotes para artefatos publicáveis:
- `files`/`exports` corretos;
- dist limpo;
- tipos incluídos;
- peer dependencies corretas;
- dependências internas versionadas corretamente;
- conteúdo do tarball verificável com `pnpm pack --dry-run` ou equivalente.

Publique no Verdaccio em ordem correta. Depois:
- substitua nos três apps os `workspace:*` por versões exatas `1.0.0` dos pacotes aplicáveis;
- configure o workspace para impedir que uma dependência escrita como versão exata seja silenciosamente ligada ao pacote local de mesmo nome; dependências internas entre os próprios pacotes podem continuar usando `workspace:*`, pois o empacotamento deve convertê-las para versões publicáveis;
- reinstale os apps a partir do registry local;
- confirme pelo lockfile, pelo endereço de resolução e por comandos `pnpm why`/equivalentes que os apps consomem artefatos publicados;
- os pacotes podem permanecer no mesmo monorepo para edição, mas os apps não devem mais resolvê-los por workspace nesta etapa.

Crie scripts raiz claros:
- `registry:up`;
- `registry:down`;
- `packages:install` para instalar somente o toolchain dos pacotes em uma máquina limpa;
- `packages:build`;
- `packages:pack:check`;
- `packages:publish:local`;
- `apps:install:local-registry`;
- `bootstrap:local`, orquestrando em ordem reproduzível: subir registry, instalar/buildar pacotes, publicar e somente então instalar os apps.

O fluxo deve funcionar também em clone limpo. Documente por que um `pnpm install` global pode depender do registry já estar populado depois que os apps passam a usar versões publicadas, e forneça o comando de bootstrap como entrada oficial.

Crie `docs/experiments/02-local-npm-registry.md` e `docs/lessons/16-real-package-publication.md` explicando:
- registry;
- tarball;
- versionamento;
- lockfile;
- publicação versus instalação;
- por que estar no mesmo repositório não muda a semântica do pacote publicado.

Execute um fluxo limpo a partir do registry local e todos os checks. Atualize `docs/progress.md`.
```

### Antes de seguir, saiba responder

- De onde o shell está recebendo `@mfe-lab/ui-react` agora?
- O código-fonte local do pacote é usado automaticamente pelo app?
- Qual artefato foi efetivamente publicado?
- O que o lockfile prova?

## Prompt 17 — Module Federation versus pacote npm

```markdown
Leia `AGENTS.md`, a lição 16 e o experimento de deploy independente do remote. Execute somente a etapa 17.

Objetivo:
Criar uma comparação controlada entre atualização por Module Federation e atualização por pacote npm.

Parte A — pacote npm:
1. mantenha shell e Products instalados com `@mfe-lab/ui-react@1.0.0`;
2. faça uma alteração pequena e retrocompatível na lib, como adicionar prop opcional `size="compact"` ao `LabButton` e atualizar `UI_REACT_VERSION`;
3. aumente a versão para `1.1.0`;
4. publique `1.1.0` no Verdaccio;
5. prove que os apps continuam usando `1.0.0` sem atualização explícita;
6. atualize somente Products para `1.1.0`, use a nova prop e rebuild Products;
7. mantenha o shell em `1.0.0`;
8. mostre visualmente as versões consumidas em cada app.

Parte B — remote:
1. altere `Remote version` de Products para `products-v3`;
2. rebuild/reinicie somente o remote;
3. confirme que o shell buildado continua carregando `products-v3` no mesmo endpoint, sem rebuild próprio.

Crie `docs/experiments/03-runtime-vs-package-update.md` com uma tabela contendo:
- artefato alterado;
- versão/endereço;
- consumidor recompilado?;
- consumidor redeployado?;
- quando a mudança apareceu;
- risco de incompatibilidade.

Crie `docs/lessons/17-module-federation-vs-npm.md` com uma explicação de entrevista em 2 minutos e exemplos do próprio laboratório.

Não atualize o shell para `ui-react@1.1.0` nesta etapa; a diferença visual é parte da evidência. Execute checks. Atualize `docs/progress.md`.
```

### Antes de seguir, saiba responder

- Por que Products recebeu `ui-react@1.1.0` somente após atualização e rebuild?
- Por que o shell viu `products-v3` sem rebuild?
- Qual abordagem oferece rollback por versão mais explícito?
- Qual abordagem aumenta o acoplamento operacional em runtime?

## Prompt 18 — Dependências compartilhadas visíveis

```markdown
Leia `AGENTS.md`, a lição 17 e a documentação oficial atual sobre `shared`, `singleton` e versionamento no Module Federation. Execute somente a etapa 18.

Objetivo:
Tornar a negociação de dependências compartilhadas visível sem depender de APIs internas instáveis.

Adicione um painel técnico pequeno e recolhível no shell, visível apenas em desenvolvimento, mostrando:
- versão do React usada pelo shell;
- versão do React informada pelo Products remote;
- versão do Vue informada pelo Account remote;
- versão do remote Products;
- versão do remote Account;
- versão de `ui-react` usada pelo shell;
- versão de `ui-react` usada por Products;
- URLs atuais dos manifests.

A informação do remote deve vir por uma exportação pública pequena ou props/eventos claramente documentados, não por leitura arbitrária de globals internos.

Revise as configurações `shared`:
- React e ReactDOM entre shell e Products;
- Vue no Account;
- `singleton` e `requiredVersion` quando cabíveis;
- não colocar automaticamente todos os pacotes em `shared`;
- não colocar `@mfe-lab/ui-react` em `shared` só para reduzir bundle.

Crie `docs/experiments/04-shared-dependencies.md` com um exercício manual, não automatizado, para:
1. observar o estado correto;
2. remover temporariamente `singleton` em uma cópia/branch local;
3. entender os possíveis sintomas;
4. restaurar a configuração correta.

Não deixe o repositório principal quebrado nem force versões incompatíveis no lockfile final.

Crie `docs/lessons/18-shared-dependencies.md` explicando:
- provider e consumer da dependência;
- share scope;
- singleton;
- required version;
- diferença entre evitar duplicidade e distribuir feature code;
- por que compartilhar tudo pode gerar acoplamento e conflitos.

Execute checks completos. Atualize `docs/progress.md`.
```

### Antes de seguir, saiba responder

- Quem fornece React em runtime?
- `singleton` garante compatibilidade semântica?
- Por que uma biblioteca de UI nem sempre deve entrar em `shared`?
- O que pode acontecer com duas cópias de React?

## Prompt 19 — Resiliência em runtime

```markdown
Leia `AGENTS.md` e as lições anteriores. Execute somente a etapa 19.

Objetivo:
Garantir que uma falha de rede, manifest ou renderização remota não derrube o shell inteiro.

Refatore as fronteiras remotas sem criar uma abstração genérica excessiva.

Products:
- loading;
- erro de carregamento;
- erro de renderização;
- botão Retry que realmente refaça a tentativa quando viável;
- mensagem contendo o nome do remote, sem stack trace para usuário.

Account Vue:
- loading do import;
- erro ao importar;
- erro durante `mount`;
- cleanup seguro em sucesso, erro e troca rápida de rota;
- Retry;
- nenhum container órfão.

Shell:
- Home e header devem continuar funcionando quando um ou ambos os remotes estiverem desligados;
- a rota com erro deve mostrar fallback local;
- logs técnicos em desenvolvimento, sem poluir produção.

Crie `docs/experiments/05-remote-failure.md` com testes manuais:
1. Products desligado e Account ligado;
2. Account desligado e Products ligado;
3. ambos desligados;
4. remote volta depois do erro e Retry funciona;
5. troca rápida de rota durante carregamento.

Crie `docs/lessons/19-runtime-resilience.md` explicando:
- acoplamento operacional;
- timeout/retry;
- fallback;
- cache;
- observabilidade;
- diferença entre falha de import e falha de renderização.

Não implemente circuit breaker complexo, service worker ou runtime plugin avançado nesta etapa. Execute checks. Atualize `docs/progress.md`.
```

### Antes de seguir, saiba responder

- Por que deploy independente cria dependência de disponibilidade em runtime?
- Qual é a diferença entre erro ao baixar o remote e erro dentro do componente?
- Retry deve recarregar a página inteira?

## Prompt 20 — Isolamento de CSS

```markdown
Leia `AGENTS.md`, a lição 19 e a documentação oficial atual sobre isolamento de estilos em Module Federation. Execute somente a etapa 20.

Objetivo:
Demonstrar que apps federados compartilham a página e podem poluir o CSS global, e depois deixar a solução correta no repositório.

Faça uma demonstração controlada:
- identifique um nome de classe comum que poderia colidir, como `.title`;
- documente em um experimento como uma regra global do remote poderia alterar o shell;
- não deixe a colisão ativa no estado final.

Estado final obrigatório:
- shell e Products com CSS Modules;
- Account com estilo Vue `scoped`;
- somente reset mínimo e design tokens no escopo global;
- raízes dos apps com atributos ou classes de ownership úteis para debug;
- `ui-web` preservando Shadow DOM;
- nenhum remote deve definir seletores globais genéricos;
- fontes e body pertencem ao shell, não aos remotes;
- design tokens continuam acessíveis.

Crie `docs/experiments/06-css-collision.md` e `docs/lessons/20-css-isolation.md` explicando:
- escopo CSS global da página;
- CSS Modules;
- Vue scoped;
- Shadow DOM;
- prefixos/namespaces;
- limites de cada abordagem.

Execute checks e validação visual das três rotas. Atualize `docs/progress.md`.
```

### Antes de seguir, saiba responder

- Module Federation isola CSS automaticamente?
- Qual parte do CSS pode continuar global?
- Qual é a principal diferença entre Vue `scoped` e Shadow DOM?

## Prompt 21 — Testes de integração

```markdown
Leia `AGENTS.md` e todas as lições. Execute somente a etapa 21.

Adicione uma suíte pequena de testes, escolhendo o menor conjunto que realmente prove a arquitetura.

Unitários com Vitest:
- contratos/type guards runtime, se existirem;
- lifecycle `mount`/`unmount` do Account em cenário isolado, quando viável sem acoplamento excessivo;
- listeners/cleanup de eventos apenas se puderem ser testados sem duplicar implementação.

E2E com Playwright:
1. Home abre no shell;
2. navegar para Products carrega a marca do remote React;
3. clicar em Adicionar atualiza `Carrinho: N` no shell;
4. navegar para Account carrega a marca do remote Vue;
5. alternar o papel atualiza o header do shell;
6. voltar e entrar novamente em Account não duplica a aplicação;
7. pelo menos um cenário de remote indisponível mostra fallback sem derrubar Home.

Requisitos:
- configurar os três servidores no fluxo E2E sem depender de execução manual confusa;
- não testar detalhes internos de framework;
- usar seletores acessíveis ou `data-testid` apenas onde necessário;
- testes determinísticos;
- sem snapshot visual;
- sem cobertura artificial de 100%;
- documentar quais riscos ainda não estão cobertos.

Crie scripts raiz `test`, `test:unit`, `test:e2e` e `check:all`.

Crie `docs/lessons/21-integration-tests.md` explicando:
- teste unitário versus contrato versus E2E;
- por que o E2E é importante em composição runtime;
- como evitar testes frágeis entre equipes.

Execute a suíte e registre resultados. Atualize `docs/progress.md`.
```

### Antes de seguir, saiba responder

- Qual teste prova que o remote foi realmente composto?
- Por que testar apenas o componente Products isolado é insuficiente?
- O que um contract test poderia verificar entre builds independentes?

## Prompt 22 — Containers independentes

```markdown
Leia `AGENTS.md`, as lições e o conhecimento existente do projeto sobre Docker. Execute somente a etapa 22.

Objetivo:
Empacotar shell, Products e Account como artefatos/container independentes de produção, sem transformar tudo em uma única imagem.

Crie:
- Dockerfile multi-stage para `shell-react`;
- Dockerfile multi-stage para `products-react`;
- Dockerfile multi-stage para `account-vue`;
- configuração Nginx mínima para SPA e assets de Module Federation;
- headers CORS e cache coerentes para manifests e chunks;
- `docker-compose.yml` do laboratório com:
  - shell em `localhost:8080`;
  - products em `localhost:8081`;
  - account em `localhost:8082`;
  - Verdaccio em `localhost:4873`, somente quando necessário ao build/publicação;
- variáveis de build/runtime claramente documentadas para URLs dos manifests.

Cuidados:
- as URLs usadas pelo navegador devem ser acessíveis pelo navegador, não apenas pelos nomes internos do Compose;
- cada app deve ter seu próprio build e imagem;
- deve ser possível rebuildar/reiniciar somente Products e observar atualização no shell;
- não colocar node_modules de desenvolvimento na imagem final;
- não servir todos os apps pelo mesmo container;
- não adicionar Kubernetes;
- não adicionar Traefik nesta etapa;
- não publicar imagens em registry externo.

Crie `docs/experiments/07-independent-containers.md` com comandos para:
1. subir tudo;
2. verificar os manifests;
3. rebuildar somente Products;
4. derrubar somente Account;
5. validar fallback no shell.

Crie `docs/lessons/22-independent-deployment.md` explicando:
- build artifact;
- imagem;
- container;
- endereço público do remote;
- cache do manifest versus chunks versionados;
- por que monorepo e deploy independente podem coexistir.

Execute builds e smoke tests. Atualize `docs/progress.md`.
```

### Antes de seguir, saiba responder

- Cada app tem sua própria imagem?
- Por que o navegador não pode usar `http://products:80` diretamente?
- Qual arquivo deveria ter cache curto: manifest ou chunk com hash?

## Prompt 23 — CI e ownership

```markdown
Leia `AGENTS.md` e a lição 22. Execute somente a etapa 23.

Crie uma CI pequena em GitHub Actions para validar o laboratório sem publicar nada.

Requisitos:
- instalar Node e pnpm nas versões registradas;
- iniciar um Verdaccio efêmero como service container ou etapa equivalente, pois os apps agora dependem de pacotes `@mfe-lab` publicados;
- reutilizar o fluxo `bootstrap:local` de forma não interativa: instalar/buildar os pacotes, publicá-los no registry efêmero e somente depois instalar os apps;
- instalação reproduzível com lockfile e verificação de que os pacotes vieram do registry da CI, não de links de workspace;
- cache apropriado do pnpm, sem cachear o storage mutável do Verdaccio como fonte de verdade;
- jobs ou matriz claros para:
  - pacotes;
  - shell;
  - Products;
  - Account;
  - E2E integrado;
- packages devem ser construídos antes dos consumidores quando necessário;
- nenhum secret real;
- nenhuma publicação no npm ou Docker Hub;
- possibilidade de usar filtros de paths apenas se não criar falso positivo de segurança; documentar dependências entre paths;
- falha de um remote deve ser identificável pelo nome do job;
- workflow legível, sem abstração excessiva.

Crie `docs/lessons/23-ci-and-ownership.md` explicando:
- ownership por domínio;
- pipeline independente versus teste integrado;
- por que mudanças em contracts/tokens podem exigir validar vários consumidores;
- por que deploy independente não elimina integração contínua.

Valide a sintaxe do workflow e execute localmente todos os comandos equivalentes. Atualize `docs/progress.md`.
```

### Antes de seguir, saiba responder

- Uma mudança em Products precisa buildar Account?
- Uma mudança em contracts precisa validar quais apps?
- Por que ainda precisamos de E2E integrado?

## Prompt 24 — Revisão final

```markdown
Leia `AGENTS.md`, todas as lições, experimentos e código atual. Execute somente a etapa 24. Esta etapa é de documentação, revisão e limpeza; não redesenhe a arquitetura.

Atualize o README para ser a entrada definitiva do laboratório, contendo:
- objetivo;
- screenshot textual/descrição mínima das três rotas;
- arquitetura;
- tabela de portas;
- comandos de desenvolvimento;
- comandos do registry local;
- testes;
- Docker;
- ordem recomendada de leitura;
- troubleshooting curto;
- comparação Module Federation versus npm.

Crie:
- `docs/glossary.md` com termos essenciais;
- `docs/diagrams/final-architecture.md` com diagramas Mermaid de build time e runtime separados;
- `docs/decisions/ADR-001-use-module-federation.md`;
- `docs/decisions/ADR-002-npm-packages-for-shared-ui.md`;
- `docs/decisions/ADR-003-custom-events-for-demo-communication.md`;
- `docs/decisions/ADR-004-manual-vue-lifecycle-boundary.md`;
- `docs/interview-guide.md` com:
  - apresentação do projeto em 30 segundos;
  - apresentação em 2 minutos;
  - 20 perguntas comuns e respostas curtas;
  - trade-offs reais;
  - sinais de quando não usar micro frontend;
  - como essa solução mudaria com times e repositórios separados.

Faça uma revisão de limpeza:
- remover arquivos mortos;
- remover dependências não usadas;
- procurar `any`, `ts-ignore`, TODOs esquecidos e logs indevidos;
- conferir nomes de remotes, portas, URLs e versões documentadas;
- garantir que nenhuma credencial ou token esteja versionado;
- executar `check:all` e build Docker final.

Atualize `docs/progress.md` marcando a trilha principal como concluída e liste os prompts opcionais ainda não executados.

Crie `docs/lessons/24-final-review.md` com:
- o que você deveria saber agora;
- lacunas restantes;
- próximos experimentos;
- checklist para explicar o projeto em entrevista.

Não crie commit. Sugira uma mensagem final. Termine com `PAREI NESTA ETAPA`.
```

### Antes de encerrar, saiba explicar

- a diferença entre MFE, Module Federation, monorepo e pacote npm;
- como o React remote é carregado;
- como o Vue é montado e desmontado;
- por que React/ReactDOM entram em `shared`;
- por que `ui-react` não entra automaticamente em `shared`;
- por que o shell vê um remote novo sem rebuild;
- por que não vê uma lib npm nova sem atualizar e rebuildar;
- como os micro frontends se comunicam sem compartilhar store;
- o impacto de falha e cache em runtime;
- quando essa arquitetura seria exagero.
